---
week: "W45"
date: 2024-11-04
---

Server/deployment troubleshooting on a DigitalOcean droplet running the tattoo-admin backend via docker-compose — installed docker-compose itself, then worked through the rebuild/redeploy cycle (`docker-compose down && docker-compose up -d --build`) against separate dev/prod compose files.
